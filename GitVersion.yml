branches:
  master:
    mode: ContinuousDeployment
    label: ''
    increment: Patch
    regex: ^master$
  develop:
    mode: ContinuousDeployment
    label: beta
    increment: Minor
    regex: ^develop$
